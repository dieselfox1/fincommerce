/**
 * External dependencies
 */
import { __ } from '@finpress/i18n';

export const defaultCartButtonLabel = __( 'View my cart', 'fincommerce' );

export const defaultCheckoutButtonLabel = __( 'Go to checkout', 'fincommerce' );
