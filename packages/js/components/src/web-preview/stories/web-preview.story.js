/**
 * External dependencies
 */
import { WebPreview } from '@fincommerce/components';

export const Basic = () => (
	<WebPreview
		src="https://themes.fincommerce.com/?name=galleria"
		title="My Web Preview"
	/>
);

export default {
	title: 'Components/WebPreview',
	component: WebPreview,
};
