/**
 * External dependencies
 */
import { Spinner } from '@fincommerce/components';

export const Basic = () => (
	<div>
		<Spinner />
	</div>
);

export default {
	title: 'Components/Spinner',
	component: Spinner,
};
